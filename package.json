{
  "name": "@dookdiks/utils",
  "version": "2.0.2",
  "description": "DukDik is a lightweight utility library that provides helpful functions for common tasks.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "build": "tsc",
    "release": "npm run build && npm publish --access=public || echo 'version already published'",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/DookDiks/utils"
  },
  "keywords": [
    "utils"
  ],
  "author": "dookdiks",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@dookdiks/error": "^1.0.2",
    "clsx": "^2.0.0",
    "tailwind-merge": "^2.0.0"
  }
}
